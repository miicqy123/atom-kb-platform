# 前端工程代码 · 编排中心 + 治理中心 + 企业后台（OC-01~04 · GC-01~04 · Admin-01~06）

<aside>
⚙️

本页包含剩余 **14 个前端页面**的完整 React 组件代码：
⚙️ 编排中心 4 页（OC-01~04）· 🛡️ 治理中心 4 页（GC-01~04）· 🏢 企业后台 6 页（Admin-01~06）

</aside>

---

## 1. ⚙️ OC-01：Agent 列表

### src/app/(dashboard)/orchestration/agents/page.tsx

```tsx
"use client";
import { trpc } from "@/lib/trpc";
import { useProjectStore } from "@/stores/projectStore";
import { PageHeader } from "@/components/ui/PageHeader";
import { StatusBadge } from "@/components/ui/StatusBadge";
import { Plus, Bot, Zap, Clock, BarChart3 } from "lucide-react";
export default function AgentsPage() {
  const { currentProject } = useProjectStore();
  const { data: agents, isLoading } = trpc.agent.list.useQuery(
    { projectId: currentProject?.id ?? "" },
    { enabled: !!currentProject }
  );
  return (
    <div>
      <PageHeader title="Agent 列表" description="管理 Agent 实例、绑定蓝图与运行统计"
        actions={<button className="flex items-center gap-2 rounded-lg bg-oc px-4 py-2 text-sm text-white hover:bg-oc/90"><Plus className="h-4 w-4" />新建 Agent</button>} />
      {isLoading ? <div className="py-12 text-center text-gray-400">加载中…</div> : (
        <div className="grid grid-cols-3 gap-4">
          {(agents ?? []).map((agent: any) => (
            <div key={agent.id} className="rounded-xl border bg-white p-5 hover:shadow-md transition-shadow cursor-pointer">
              <div className="flex items-center justify-between mb-3">
                <div className="flex items-center gap-2">
                  <div className="h-10 w-10 rounded-xl bg-oc/10 flex items-center justify-center"><Bot className="h-5 w-5 text-oc" /></div>
                  <div><h3 className="font-semibold text-sm">{agent.name}</h3><p className="text-xs text-gray-400">{agent.description ?? "无描述"}</p></div>
                </div>
                <StatusBadge status={agent.status === "online" ? "SUCCESS" : agent.status === "maintenance" ? "CONVERTING" : "FAILED"} />
              </div>
              <div className="mb-3">
                <p className="text-xs text-gray-500 mb-1">绑定蓝图</p>
                <div className="flex flex-wrap gap-1">
                  {agent.blueprints?.map((ab: any) => (
                    <span key={ab.blueprintId} className="rounded bg-oc/10 px-2 py-0.5 text-xs text-oc">{ab.blueprint?.name}</span>
                  ))}
                  {(!agent.blueprints || agent.blueprints.length === 0) && <span className="text-xs text-gray-300">未绑定</span>}
                </div>
              </div>
              <div className="grid grid-cols-4 gap-2 text-center">
                <div><Zap className="mx-auto h-4 w-4 text-gray-300 mb-0.5" /><p className="text-xs font-bold">{agent.totalRuns}</p><p className="text-[10px] text-gray-400">总运行</p></div>
                <div><BarChart3 className="mx-auto h-4 w-4 text-gray-300 mb-0.5" /><p className="text-xs font-bold">{agent.successRate ? (agent.successRate * 100).toFixed(0) + "%" : "—"}</p><p className="text-[10px] text-gray-400">成功率</p></div>
                <div><Clock className="mx-auto h-4 w-4 text-gray-300 mb-0.5" /><p className="text-xs font-bold">{agent.avgDuration ? agent.avgDuration.toFixed(1) + "s" : "—"}</p><p className="text-[10px] text-gray-400">平均耗时</p></div>
                <div><span className="mx-auto block h-4 w-4 text-center text-gray-300 text-xs mb-0.5">T</span><p className="text-xs font-bold">{agent.avgTokens ? Math.round(agent.avgTokens) : "—"}</p><p className="text-[10px] text-gray-400">平均Token</p></div>
              </div>
            </div>
          ))}
        </div>
      )}
    </div>
  );
}
```

---

## 2. ⚙️ OC-02：Workflow 模板管理

### src/app/(dashboard)/orchestration/workflows/page.tsx

```tsx
"use client";
import { useState } from "react";
import { PageHeader } from "@/components/ui/PageHeader";
import { Plus, GitBranch, RefreshCw, Users, Database } from "lucide-react";
const MODES = [
  { key: "DAG", label: "DAG 有向无环图", icon: <GitBranch className="h-5 w-5" />, desc: "固定节点顺序执行，适合标准化流程", color: "border-blue-200 bg-blue-50" },
  { key: "REACT", label: "ReAct 推理-行动", icon: <RefreshCw className="h-5 w-5" />, desc: "思考→行动→观察循环，适合探索性任务", color: "border-purple-200 bg-purple-50" },
  { key: "ROLE_COLLABORATION", label: "角色协作", icon: <Users className="h-5 w-5" />, desc: "多角色协作分工，适合复杂内容生产", color: "border-green-200 bg-green-50" },
  { key: "STATEFUL_GRAPH", label: "有状态图+HITL", icon: <Database className="h-5 w-5" />, desc: "支持断点恢复和人工接管，适合高风险任务", color: "border-orange-200 bg-orange-50" },
];
export default function WorkflowsPage() {
  const [activeMode, setActiveMode] = useState("DAG");
  return (
    <div>
      <PageHeader title="Workflow 模板管理" description="管理 4 种 workflow_mode 的节点编排模板"
        actions={<button className="flex items-center gap-2 rounded-lg bg-oc px-4 py-2 text-sm text-white"><Plus className="h-4 w-4" />新建模板</button>} />
      <div className="grid grid-cols-4 gap-3 mb-6">
        {MODES.map(m => (
          <button key={m.key} onClick={() => setActiveMode(m.key)}
            className={`rounded-xl border-2 p-4 text-left transition-all ${activeMode === m.key ? m.color + " ring-2 ring-oc" : "border-gray-100 bg-white hover:border-gray-200"}`}>
            <div className="flex items-center gap-2 mb-2">{m.icon}<span className="font-semibold text-sm">{m.label}</span></div>
            <p className="text-xs text-gray-500">{m.desc}</p>
          </button>
        ))}
      </div>
      <div className="rounded-xl border bg-white p-6">
        <h3 className="text-sm font-semibold mb-4">可视化节点编辑器 — {MODES.find(m => m.key === activeMode)?.label}</h3>
        <div className="h-80 rounded-lg bg-gray-50 border-2 border-dashed border-gray-200 flex items-center justify-center">
          <div className="text-center text-gray-400">
            <GitBranch className="mx-auto h-12 w-12 mb-2" />
            <p className="text-sm">拖拽节点到画布以配置 Workflow</p>
            <p className="text-xs mt-1">每个节点可绑定蓝图/工具/人工审核点</p>
            <div className="mt-4 flex gap-2 justify-center">
              <span className="rounded border px-2 py-1 text-xs">🎯 Planner</span>
              <span className="rounded border px-2 py-1 text-xs">⚡ Generator</span>
              <span className="rounded border px-2 py-1 text-xs">✅ Evaluator</span>
              <span className="rounded border px-2 py-1 text-xs">🤝 Handoff</span>
              <span className="rounded border px-2 py-1 text-xs">👤 HITL</span>
            </div>
          </div>
        </div>
        <div className="mt-4 grid grid-cols-3 gap-4">
          <div className="rounded-lg border p-3"><h4 className="text-xs font-semibold mb-1">3A 闭环配置</h4><p className="text-xs text-gray-500">每个节点的 Planner → Generator → Evaluator 参数</p></div>
          <div className="rounded-lg border p-3"><h4 className="text-xs font-semibold mb-1">Handoff 协议</h4><p className="text-xs text-gray-500">节点间消息格式预览</p></div>
          <div className="rounded-lg border p-3"><h4 className="text-xs font-semibold mb-1">Checkpointer</h4><p className="text-xs text-gray-500">stateful_graph 持久化 + 人工接管点</p></div>
        </div>
      </div>
    </div>
  );
}
```

---

## 3. ⚙️ OC-03：运行记录 Dashboard

### src/app/(dashboard)/orchestration/runs/page.tsx

```tsx
"use client";
import { useState } from "react";
import { trpc } from "@/lib/trpc";
import { useProjectStore } from "@/stores/projectStore";
import { PageHeader } from "@/components/ui/PageHeader";
import { StatsCard } from "@/components/ui/StatsCard";
import { DataTable, type Column } from "@/components/ui/DataTable";
import { StatusBadge } from "@/components/ui/StatusBadge";
import { Pagination } from "@/components/ui/Pagination";
import { formatPercent } from "@/lib/utils";
import { Activity, CheckCircle, Clock, Coins, Zap } from "lucide-react";
export default function RunsPage() {
  const { currentProject } = useProjectStore();
  const [page, setPage] = useState(1);
  const [statusFilter, setStatusFilter] = useState("");
  const stats = trpc.workflowRun.todayStats.useQuery({ projectId: currentProject?.id ?? "" }, { enabled: !!currentProject });
  const { data, isLoading } = trpc.workflowRun.list.useQuery({ projectId: currentProject?.id ?? "", page, status: statusFilter || undefined }, { enabled: !!currentProject });
  const columns: Column<any>[] = [
    { key: "id", label: "执行 ID", render: (r) => <span className="font-mono text-xs text-brand">{r.id.slice(0, 8)}…</span> },
    { key: "blueprint", label: "Blueprint", render: (r) => <span className="text-sm">{r.blueprint?.name} <span className="text-xs text-gray-400">v{r.blueprintVersion}</span></span> },
    { key: "workflowMode", label: "Mode", render: (r) => <span className="rounded bg-oc/10 px-1.5 py-0.5 text-xs font-mono text-oc">{r.workflowMode}</span> },
    { key: "status", label: "状态", render: (r) => <StatusBadge status={r.status} /> },
    { key: "tokenUsage", label: "Token", render: (r) => <span className="text-xs">{r.tokenUsage?.toLocaleString() ?? "—"}</span> },
    { key: "duration", label: "耗时", render: (r) => <span className="text-xs">{r.duration ? r.duration.toFixed(1) + "s" : "—"}</span> },
    { key: "cost", label: "成本", render: (r) => <span className="text-xs">{r.cost ? "¥" + r.cost.toFixed(2) : "—"}</span> },
    { key: "businessResult", label: "回流", render: (r) => (
      <div className="flex gap-1">
        <span className={`h-2 w-2 rounded-full ${r.contentPerformance ? "bg-green-400" : "bg-gray-200"}`} title="内容" />
        <span className={`h-2 w-2 rounded-full ${r.roiMetrics ? "bg-green-400" : "bg-gray-200"}`} title="ROI" />
        <span className={`h-2 w-2 rounded-full ${r.conversionOutcome ? "bg-green-400" : "bg-gray-200"}`} title="客资" />
      </div>
    )},
    { key: "startedAt", label: "时间", render: (r) => <span className="text-xs text-gray-400">{new Date(r.startedAt).toLocaleString("zh-CN")}</span> },
  ];
  return (
    <div>
      <PageHeader title="运行记录 Dashboard" description="WorkflowRun 执行记录、Trace 视图与业务结果回流" />
      <div className="grid grid-cols-5 gap-4 mb-6">
        <StatsCard title="今日运行数" value={stats.data?.total ?? 0} icon={<Activity className="h-5 w-5 text-oc" />} color="text-oc" />
        <StatsCard title="成功率" value={stats.data ? formatPercent(stats.data.successRate) : "—"} icon={<CheckCircle className="h-5 w-5 text-green-500" />} color="text-green-600" />
        <StatsCard title="平均耗时" value={stats.data ? stats.data.avgDuration.toFixed(1) + "s" : "—"} icon={<Clock className="h-5 w-5 text-blue-500" />} color="text-blue-600" />
        <StatsCard title="总 Token" value={stats.data?.totalTokens?.toLocaleString() ?? "—"} icon={<Zap className="h-5 w-5 text-purple-500" />} color="text-purple-600" />
        <StatsCard title="总成本" value={stats.data ? "¥" + stats.data.totalCost.toFixed(2) : "—"} icon={<Coins className="h-5 w-5 text-orange-500" />} color="text-orange-600" />
      </div>
      <div className="mb-4">
        <select value={statusFilter} onChange={e => setStatusFilter(e.target.value)} className="h-9 rounded-lg border px-3 text-sm">
          <option value="">全部状态</option>{["RUNNING","SUCCESS","FAILED","DEGRADED","HUMAN_TAKEOVER"].map(s => <option key={s} value={s}>{s}</option>)}
        </select>
      </div>
      <DataTable columns={columns} data={data?.items ?? []} loading={isLoading} />
      {data && <Pagination page={page} totalPages={data.totalPages} onChange={setPage} />}
    </div>
  );
}
```

---

## 4. ⚙️ OC-04：HITL 人工接管队列

### src/app/(dashboard)/orchestration/review/page.tsx

```tsx
"use client";
import { useState } from "react";
import { trpc } from "@/lib/trpc";
import { useProjectStore } from "@/stores/projectStore";
import { PageHeader } from "@/components/ui/PageHeader";
import { StatusBadge } from "@/components/ui/StatusBadge";
import { AlertCircle, CheckCircle, Edit3, ArrowUp, X } from "lucide-react";
export default function ReviewPage() {
  const { currentProject } = useProjectStore();
  const { data: queue } = trpc.review.queue.useQuery({ projectId: currentProject?.id ?? "" }, { enabled: !!currentProject });
  const resolve = trpc.review.resolve.useMutation();
  const [selected, setSelected] = useState<any>(null);
  const [editOutput, setEditOutput] = useState("");
  const urgencyColor = (u: string) => u === "RED" ? "bg-red-500" : u === "YELLOW" ? "bg-yellow-400" : "bg-green-400";
  return (
    <div>
      <PageHeader title="HITL 人工接管队列" description="处理 S8 不通过 / 红线冲突 / 置信度极低 / 重试超限的人工审核任务" />
      <div className="grid grid-cols-5 gap-6">
        <div className="col-span-2 space-y-2">
          <h3 className="text-sm font-semibold mb-2">待审核队列 ({(queue ?? []).length})</h3>
          {(queue ?? []).map((task: any) => (
            <div key={task.id} onClick={() => { setSelected(task); setEditOutput(task.workflowRun?.outputContent ?? ""); }}
              className={`rounded-lg border p-3 cursor-pointer transition-colors ${selected?.id === task.id ? "ring-2 ring-oc border-oc" : "hover:bg-gray-50"}`}>
              <div className="flex items-center justify-between mb-1">
                <div className="flex items-center gap-2">
                  <span className={`h-2.5 w-2.5 rounded-full ${urgencyColor(task.urgency)}`} />
                  <span className="text-sm font-medium">{task.workflowRun?.blueprint?.name}</span>
                </div>
                <StatusBadge status={task.status} />
              </div>
              <p className="text-xs text-gray-500">触发原因: {task.triggerReason}</p>
              <p className="text-xs text-gray-400">排队: {new Date(task.createdAt).toLocaleString("zh-CN")}</p>
              {task.assignee && <p className="text-xs text-gray-400">处理人: {task.assignee.name}</p>}
            </div>
          ))}
          {(!queue || queue.length === 0) && <p className="text-sm text-gray-400 text-center py-8">🎉 队列已清空</p>}
        </div>
        <div className="col-span-3">
          {selected ? (
            <div className="rounded-xl border bg-white p-4">
              <h3 className="text-sm font-semibold mb-4">审核工作台</h3>
              <div className="grid grid-cols-2 gap-4 mb-4">
                <div className="rounded-lg bg-gray-50 p-3">
                  <h4 className="text-xs font-semibold text-gray-500 mb-2">原始输入 + 上下文</h4>
                  <pre className="text-xs font-mono whitespace-pre-wrap max-h-40 overflow-y-auto">{JSON.stringify(selected.workflowRun?.inputData, null, 2) ?? "无输入数据"}</pre>
                </div>
                <div className="rounded-lg bg-yellow-50 p-3">
                  <h4 className="text-xs font-semibold text-yellow-700 mb-2">模型输出（含质检标记）</h4>
                  <textarea value={editOutput} onChange={e => setEditOutput(e.target.value)} className="w-full h-32 text-xs font-mono border rounded p-2 bg-white" />
                </div>
              </div>
              <div className="flex gap-2">
                <button onClick={() => resolve.mutate({ id: selected.id, status: "APPROVED" })} className="flex items-center gap-1 rounded-lg bg-green-600 px-4 py-2 text-xs text-white hover:bg-green-700"><CheckCircle className="h-3.5 w-3.5" />通过</button>
                <button onClick={() => resolve.mutate({ id: selected.id, status: "MODIFIED_APPROVED", modifiedOutput: editOutput })} className="flex items-center gap-1 rounded-lg bg-blue-600 px-4 py-2 text-xs text-white hover:bg-blue-700"><Edit3 className="h-3.5 w-3.5" />修改后通过</button>
                <button onClick={() => resolve.mutate({ id: selected.id, status: "REJECTED", feedback: "质量不达标" })} className="flex items-center gap-1 rounded-lg bg-red-600 px-4 py-2 text-xs text-white hover:bg-red-700"><X className="h-3.5 w-3.5" />驳回</button>
                <button onClick={() => resolve.mutate({ id: selected.id, status: "ESCALATED" })} className="flex items-center gap-1 rounded-lg border px-4 py-2 text-xs hover:bg-gray-50"><ArrowUp className="h-3.5 w-3.5" />升级</button>
              </div>
            </div>
          ) : (
            <div className="rounded-xl border bg-white p-12 text-center text-gray-400">
              <AlertCircle className="mx-auto h-10 w-10 mb-2" />
              <p>选择左侧任务以进入审核工作台</p>
            </div>
          )}
        </div>
      </div>
    </div>
  );
}
```

---

## 5. 🛡️ GC-01：可观测性大盘（4 看板）

### src/app/(dashboard)/governance/dashboard/page.tsx

```tsx
"use client";
import { useState } from "react";
import { trpc } from "@/lib/trpc";
import { useProjectStore } from "@/stores/projectStore";
import { PageHeader } from "@/components/ui/PageHeader";
import { StatsCard } from "@/components/ui/StatsCard";
import { LineChart, Line, BarChart, Bar, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer, PieChart, Pie, Cell } from "recharts";
import { Activity, Shield, Coins, AlertTriangle } from "lucide-react";
const COLORS = ["#2563EB", "#8B5CF6", "#F59E0B", "#16A34A", "#DC2626"];
const MOCK_RUN_TREND = Array.from({ length: 14 }, (_, i) => ({ day: `4/${i + 1}`, runs: 30 + Math.floor(Math.random() * 40), success: 25 + Math.floor(Math.random() * 30) }));
const MOCK_COST_PIE = [{ name: "GPT-4o", value: 45 }, { name: "Claude-3.5", value: 30 }, { name: "DeepSeek-V3", value: 20 }, { name: "其他", value: 5 }];
export default function GovernanceDashboard() {
  const [tab, setTab] = useState(0);
  const TABS = ["装配运行", "质量评分", "成本大盘", "红线异常"];
  const { currentProject } = useProjectStore();
  const incidents = trpc.analytics.incidentList.useQuery({}, { enabled: tab === 3 });
  return (
    <div>
      <PageHeader title="可观测性大盘" description="4 看板：装配运行 / 质量评分 / 成本 / 红线与异常" />
      <div className="mb-4 flex gap-1">
        {TABS.map((t, i) => (
          <button key={t} onClick={() => setTab(i)} className={`rounded-lg px-4 py-2 text-sm ${tab === i ? "bg-gc text-white" : "border hover:bg-gray-50"}`}>
            {[<Activity key={0} className="inline h-4 w-4 mr-1" />, <Shield key={1} className="inline h-4 w-4 mr-1" />, <Coins key={2} className="inline h-4 w-4 mr-1" />, <AlertTriangle key={3} className="inline h-4 w-4 mr-1" />][i]}{t}
          </button>
        ))}
      </div>
      {tab === 0 && (
        <div className="space-y-6">
          <div className="grid grid-cols-3 gap-4">
            <StatsCard title="日均运行次数" value="52" color="text-gc" />
            <StatsCard title="成功率" value="94.2%" color="text-green-600" />
            <StatsCard title="重试率" value="8.1%" color="text-orange-600" />
          </div>
          <div className="rounded-xl border bg-white p-4">
            <h3 className="text-sm font-semibold mb-3">每日运行次数与成功率趋势</h3>
            <ResponsiveContainer width="100%" height={280}>
              <BarChart data={MOCK_RUN_TREND}>
                <CartesianGrid strokeDasharray="3 3" /><XAxis dataKey="day" tick=fontSize:10 /><YAxis tick=fontSize:10 /><Tooltip />
                <Bar dataKey="runs" fill="#2563EB" name="总运行" radius={[4,4,0,0]} />
                <Bar dataKey="success" fill="#16A34A" name="成功" radius={[4,4,0,0]} />
              </BarChart>
            </ResponsiveContainer>
          </div>
        </div>
      )}
      {tab === 1 && (
        <div className="space-y-6">
          <div className="grid grid-cols-3 gap-4">
            <StatsCard title="S8 通过率" value="89.3%" color="text-brand" />
            <StatsCard title="S9 综合评分" value="83.7" color="text-pf" />
            <StatsCard title="告警线" value="<70% 触发" color="text-danger" />
          </div>
          <div className="rounded-xl border bg-white p-4">
            <h3 className="text-sm font-semibold mb-3">S8 通过率 / S9 综合评分趋势</h3>
            <ResponsiveContainer width="100%" height={280}>
              <LineChart data={MOCK_RUN_TREND.map(d => ({ ...d, s8: 80 + Math.random() * 15, s9: 75 + Math.random() * 20 }))}>
                <CartesianGrid strokeDasharray="3 3" /><XAxis dataKey="day" tick=fontSize:10 /><YAxis domain={[0, 100]} tick=fontSize:10 /><Tooltip />
                <Line type="monotone" dataKey="s8" stroke="#2563EB" strokeWidth={2} name="S8 通过率" />
                <Line type="monotone" dataKey="s9" stroke="#8B5CF6" strokeWidth={2} name="S9 评分" />
              </LineChart>
            </ResponsiveContainer>
          </div>
        </div>
      )}
      {tab === 2 && (
        <div className="space-y-6">
          <div className="grid grid-cols-3 gap-4">
            <StatsCard title="月成本" value="¥12,350" color="text-orange-600" />
            <StatsCard title="日均 Token" value="248K" color="text-pf" />
            <StatsCard title="超限告警" value="1 次" color="text-danger" />
          </div>
          <div className="grid grid-cols-2 gap-6">
            <div className="rounded-xl border bg-white p-4">
              <h3 className="text-sm font-semibold mb-3">各模型 Token 消耗占比</h3>
              <ResponsiveContainer width="100%" height={250}>
                <PieChart><Pie data={MOCK_COST_PIE} cx="50%" cy="50%" outerRadius={80} dataKey="value" label={({name, percent}: any) => `${name} ${(percent*100).toFixed(0)}%`}>
                  {MOCK_COST_PIE.map((_, i) => <Cell key={i} fill={COLORS[i % COLORS.length]} />)}
                </Pie><Tooltip /></PieChart>
              </ResponsiveContainer>
            </div>
            <div className="rounded-xl border bg-white p-4">
              <h3 className="text-sm font-semibold mb-3">成本 Top5 蓝图</h3>
              <div className="space-y-2">
                {["内容策划蓝图","话术生成蓝图","数据分析蓝图","SEO蓝图","投放策略蓝图"].map((n, i) => (
                  <div key={n} className="flex items-center justify-between rounded-lg bg-gray-50 px-3 py-2">
                    <span className="text-sm">{n}</span>
                    <span className="text-xs font-mono text-orange-600">¥{(3000 - i * 500).toLocaleString()}</span>
                  </div>
                ))}
              </div>
            </div>
          </div>
        </div>
      )}
      {tab === 3 && (
        <div className="rounded-xl border bg-white p-4">
          <h3 className="text-sm font-semibold mb-3">红线与异常事件列表</h3>
          <div className="space-y-2">
            {(incidents.data ?? []).map((inc: any) => (
              <div key={inc.id} className="flex items-center justify-between rounded-lg border p-3">
                <div className="flex items-center gap-3">
                  <span className={`rounded px-2 py-0.5 text-xs font-medium ${inc.severity === "CRITICAL" ? "bg-red-100 text-red-700" : inc.severity === "HIGH" ? "bg-orange-100 text-orange-700" : "bg-yellow-100 text-yellow-700"}`}>{inc.severity}</span>
                  <div><p className="text-sm">{inc.type}</p><p className="text-xs text-gray-400">{new Date(inc.createdAt).toLocaleString("zh-CN")}</p></div>
                </div>
                <StatusBadge status={inc.status === "resolved" ? "SUCCESS" : inc.status === "in_progress" ? "RUNNING" : "PENDING"} />
              </div>
            ))}
            {(!incidents.data || incidents.data.length === 0) && <p className="text-sm text-gray-400 text-center py-8">暂无异常事件</p>}
          </div>
        </div>
      )}
    </div>
  );
}
```

---

## 6. 🛡️ GC-02：元编排 8 角色 SLA 监控

### src/app/(dashboard)/governance/sla/page.tsx

```tsx
"use client";
import { PageHeader } from "@/components/ui/PageHeader";
const ROLES = [
  { name: "agent-organizer", desc: "蓝图匹配与 Agent 组建", metrics: ["匹配准确率", "组建延迟"], sla: ["> 95%", "< 500ms"], current: ["96.2%", "380ms"], ok: [true, true] },
  { name: "context-manager", desc: "上下文检索与缓存管理", metrics: ["检索延迟", "缓存命中率"], sla: ["< 100ms", "> 85%"], current: ["72ms", "88.5%"], ok: [true, true] },
  { name: "task-distributor", desc: "任务分发与队列管理", metrics: ["分发延迟", "队列积压"], sla: ["< 200ms", "< 50"], current: ["145ms", "12"], ok: [true, true] },
  { name: "multi-agent-coordinator", desc: "多 Agent 交接协调", metrics: ["交接成功率", "数据丢失率"], sla: ["> 99%", "= 0"], current: ["99.5%", "0"], ok: [true, true] },
  { name: "workflow-orchestrator", desc: "Workflow 编排执行", metrics: ["编排完成率", "断点恢复率"], sla: ["> 85%", "> 95%"], current: ["87.3%", "97.1%"], ok: [true, true] },
  { name: "error-coordinator", desc: "异常处理与降级", metrics: ["自动恢复率", "人工升级响应"], sla: ["> 80%", "< 5min"], current: ["82.5%", "3.2min"], ok: [true, true] },
  { name: "performance-monitor", desc: "性能采集与告警", metrics: ["采集覆盖率", "告警延迟"], sla: ["100%", "< 30s"], current: ["100%", "18s"], ok: [true, true] },
  { name: "knowledge-synthesizer", desc: "知识沉淀与复用", metrics: ["沉淀覆盖率", "复用率"], sla: ["> 90%", "持续提升"], current: ["91.2%", "↑ 5.3%"], ok: [true, true] },
];
export default function SLAPage() {
  return (
    <div>
      <PageHeader title="元编排 8 角色 SLA 监控" description="§10A 定义的 8 角色实时 SLA 仪表盘" />
      <div className="grid grid-cols-2 gap-4">
        {ROLES.map(role => (
          <div key={role.name} className="rounded-xl border bg-white p-4">
            <div className="flex items-center justify-between mb-2">
              <div><h3 className="font-semibold text-sm font-mono text-gc">{role.name}</h3><p className="text-xs text-gray-500">{role.desc}</p></div>
              <span className="text-lg">{role.ok.every(Boolean) ? "🟢" : role.ok.some(Boolean) ? "🟡" : "🔴"}</span>
            </div>
            <div className="space-y-2">
              {role.metrics.map((m, i) => (
                <div key={m} className="flex items-center justify-between rounded-lg bg-gray-50 px-3 py-2">
                  <span className="text-xs text-gray-600">{m}</span>
                  <div className="flex items-center gap-3">
                    <span className="text-xs text-gray-400">SLA: {role.sla[i]}</span>
                    <span className={`text-xs font-bold ${role.ok[i] ? "text-green-600" : "text-red-600"}`}>{role.current[i]}</span>
                  </div>
                </div>
              ))}
            </div>
          </div>
        ))}
      </div>
    </div>
  );
}
```

---

## 7. 🛡️ GC-03：审计日志

### src/app/(dashboard)/governance/audit/page.tsx

```tsx
"use client";
import { useState } from "react";
import { trpc } from "@/lib/trpc";
import { PageHeader } from "@/components/ui/PageHeader";
import { DataTable, type Column } from "@/components/ui/DataTable";
import { Pagination } from "@/components/ui/Pagination";
import { Search, ChevronDown } from "lucide-react";
export default function AuditPage() {
  const [page, setPage] = useState(1);
  const [entityType, setEntityType] = useState("");
  const { data, isLoading } = trpc.auditLog.list.useQuery({ entityType: entityType || undefined, page });
  const [expanded, setExpanded] = useState<string | null>(null);
  const columns: Column<any>[] = [
    { key: "createdAt", label: "时间", render: (r) => <span className="text-xs text-gray-500">{new Date(r.createdAt).toLocaleString("zh-CN")}</span> },
    { key: "user", label: "操作人", render: (r) => <span className="text-sm">{r.user?.name ?? "系统"}</span> },
    { key: "action", label: "操作", render: (r) => <span className={`rounded px-1.5 py-0.5 text-xs font-medium ${r.action === "create" ? "bg-green-100 text-green-700" : r.action === "delete" ? "bg-red-100 text-red-700" : r.action === "review" ? "bg-purple-100 text-purple-700" : "bg-blue-100 text-blue-700"}`}>{r.action}</span> },
    { key: "entityType", label: "资产类型", render: (r) => <span className="text-xs font-mono">{r.entityType}</span> },
    { key: "entityName", label: "资产名称", render: (r) => <span className="text-sm">{r.entityName ?? r.entityId.slice(0, 8)}</span> },
    { key: "changeSummary", label: "变更摘要", render: (r) => <span className="text-xs text-gray-500">{r.changeSummary ?? "—"}</span> },
    { key: "diff", label: "详情", render: (r) => r.diff ? (
      <button onClick={(e) => { e.stopPropagation(); setExpanded(expanded === r.id ? null : r.id); }} className="rounded border px-2 py-0.5 text-xs hover:bg-gray-50">
        <ChevronDown className={`inline h-3 w-3 transition-transform ${expanded === r.id ? "rotate-180" : ""}`} /> Diff
      </button>
    ) : <span className="text-xs text-gray-300">—</span> },
  ];
  return (
    <div>
      <PageHeader title="审计日志" description="全操作日志，支持 Diff 详情与 trace_id 关联" />
      <div className="mb-4 flex gap-3">
        <select value={entityType} onChange={e => setEntityType(e.target.value)} className="h-9 rounded-lg border px-3 text-sm">
          <option value="">全部资产类型</option>
          {["raw","atom","qa_pair","blueprint","agent","review_task","user","project"].map(t => <option key={t} value={t}>{t}</option>)}
        </select>
      </div>
      <DataTable columns={columns} data={data?.items ?? []} loading={isLoading} />
      {data && <Pagination page={page} totalPages={Math.ceil(data.total / 50)} onChange={setPage} />}
    </div>
  );
}
```

---

## 8. 🛡️ GC-04：模型网关配置

### src/app/(dashboard)/governance/gateway/page.tsx

```tsx
"use client";
import { trpc } from "@/lib/trpc";
import { PageHeader } from "@/components/ui/PageHeader";
import { Save } from "lucide-react";
const SCENE_LABELS: Record<string, string> = { routing: "路由(S6)/预检(S3)", pre_check: "预检(S3)", main_creative: "主执行·创作类(S5)", main_analytical: "主执行·分析类(S5)", script: "话术生成", evaluation: "对抗验证(S8)/评分(S9)", planner: "Planner/Evaluator(3A)" };
export default function GatewayPage() {
  const { data: configs } = trpc.modelConfig.list.useQuery({});
  const update = trpc.modelConfig.update.useMutation();
  return (
    <div>
      <PageHeader title="模型网关配置" description="§18 场景温度矩阵 · 模型管理 · 成本预算" />
      <div className="rounded-xl border bg-white overflow-hidden mb-6">
        <table className="w-full text-sm">
          <thead><tr className="bg-gray-50 border-b">
            <th className="px-4 py-3 text-left text-xs font-semibold">场景</th>
            <th className="px-4 py-3 text-left text-xs font-semibold">默认模型</th>
            <th className="px-4 py-3 text-left text-xs font-semibold">温度</th>
            <th className="px-4 py-3 text-left text-xs font-semibold">操作</th>
          </tr></thead>
          <tbody>
            {(configs ?? []).map((c: any) => (
              <tr key={c.id} className="border-b hover:bg-gray-50">
                <td className="px-4 py-3 font-medium">{SCENE_LABELS[c.scene] ?? c.scene}</td>
                <td className="px-4 py-3 font-mono text-xs">{c.defaultModel}</td>
                <td className="px-4 py-3"><span className="rounded bg-blue-50 px-2 py-0.5 text-xs font-mono text-brand">{c.temperature}</span></td>
                <td className="px-4 py-3"><button className="rounded border px-2 py-1 text-xs hover:bg-gray-50">编辑</button></td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
      <div className="grid grid-cols-2 gap-6">
        <div className="rounded-xl border bg-white p-4">
          <h3 className="text-sm font-semibold mb-3">已接入模型列表</h3>
          <div className="space-y-2">
            {["GPT-4o","GPT-4o-mini","Claude 3.5 Sonnet","DeepSeek-V3"].map(m => (
              <div key={m} className="flex items-center justify-between rounded-lg bg-gray-50 px-3 py-2">
                <span className="text-sm">{m}</span><span className="h-2 w-2 rounded-full bg-green-400" />
              </div>
            ))}
          </div>
        </div>
        <div className="rounded-xl border bg-white p-4">
          <h3 className="text-sm font-semibold mb-3">成本预算配置</h3>
          <div className="space-y-3">
            <div><label className="text-xs text-gray-500">月 Token 预算上限</label><input defaultValue="10,000,000" className="mt-1 w-full rounded-lg border px-3 py-2 text-sm" /></div>
            <div><label className="text-xs text-gray-500">超限策略</label>
              <select className="mt-1 w-full rounded-lg border px-3 py-2 text-sm"><option>告警</option><option>限流</option><option>降级到低成本模型</option></select>
            </div>
            <button className="flex items-center gap-1 rounded-lg bg-gc px-4 py-2 text-xs text-white"><Save className="h-3 w-3" />保存</button>
          </div>
        </div>
      </div>
    </div>
  );
}
```

---

## 9. 🏢 Admin-01：租户管理

### src/app/(dashboard)/admin/tenants/page.tsx

```tsx
"use client";
import { trpc } from "@/lib/trpc";
import { PageHeader } from "@/components/ui/PageHeader";
import { DataTable, type Column } from "@/components/ui/DataTable";
import { Plus } from "lucide-react";
export default function TenantsPage() {
  const { data: tenants, isLoading } = trpc.tenant.list.useQuery();
  const columns: Column<any>[] = [
    { key: "name", label: "租户名称", render: (r) => <span className="font-medium">{r.name}</span> },
    { key: "industry", label: "行业", render: (r) => <span className="text-xs">{r.industry ?? "—"}</span> },
    { key: "contactEmail", label: "联系人", render: (r) => <span className="text-xs">{r.contactEmail ?? "—"}</span> },
    { key: "workspaces", label: "Workspace", render: (r) => <span className="text-xs">{r._count?.workspaces ?? 0}</span> },
    { key: "users", label: "用户数", render: (r) => <span className="text-xs">{r._count?.users ?? 0}</span> },
    { key: "status", label: "状态", render: (r) => <span className={`rounded px-2 py-0.5 text-xs ${r.status === "active" ? "bg-green-100 text-green-700" : "bg-gray-100"}`}>{r.status}</span> },
    { key: "createdAt", label: "创建时间", render: (r) => <span className="text-xs text-gray-400">{new Date(r.createdAt).toLocaleDateString()}</span> },
  ];
  return (
    <div>
      <PageHeader title="租户管理" description="管理平台租户、配额与 Workspace"
        actions={<button className="flex items-center gap-2 rounded-lg bg-admin px-4 py-2 text-sm text-white"><Plus className="h-4 w-4" />新建租户</button>} />
      <DataTable columns={columns} data={tenants ?? []} loading={isLoading} />
    </div>
  );
}
```

---

## 10. 🏢 Admin-02：Workspace 管理

### src/app/(dashboard)/admin/workspaces/page.tsx

```tsx
"use client";
import { PageHeader } from "@/components/ui/PageHeader";
import { Plus, Users, FolderKanban } from "lucide-react";
const MOCK_WS = [
  { id: "1", name: "默认工作空间", projects: 5, members: 12, assets: 1250, createdAt: "2026-01-15" },
  { id: "2", name: "创意团队", projects: 3, members: 8, assets: 680, createdAt: "2026-02-20" },
];
export default function WorkspacesPage() {
  return (
    <div>
      <PageHeader title="Workspace 管理" description="工作空间与成员管理"
        actions={<button className="flex items-center gap-2 rounded-lg bg-admin px-4 py-2 text-sm text-white"><Plus className="h-4 w-4" />新建 Workspace</button>} />
      <div className="grid grid-cols-2 gap-4">
        {MOCK_WS.map(ws => (
          <div key={ws.id} className="rounded-xl border bg-white p-5 hover:shadow-md transition-shadow cursor-pointer">
            <h3 className="font-semibold text-sm mb-3">{ws.name}</h3>
            <div className="grid grid-cols-3 gap-4 text-center">
              <div><FolderKanban className="mx-auto h-5 w-5 text-gray-300 mb-1" /><p className="text-lg font-bold">{ws.projects}</p><p className="text-xs text-gray-400">Projects</p></div>
              <div><Users className="mx-auto h-5 w-5 text-gray-300 mb-1" /><p className="text-lg font-bold">{ws.members}</p><p className="text-xs text-gray-400">成员</p></div>
              <div><span className="mx-auto block h-5 w-5 text-center text-gray-300 text-xs mb-1">📦</span><p className="text-lg font-bold">{ws.assets}</p><p className="text-xs text-gray-400">资产</p></div>
            </div>
            <p className="mt-3 text-xs text-gray-400">创建于 {ws.createdAt}</p>
          </div>
        ))}
      </div>
    </div>
  );
}
```

---

## 11. 🏢 Admin-03：Project 管理

### src/app/(dashboard)/admin/projects/page.tsx

```tsx
"use client";
import { useState } from "react";
import { trpc } from "@/lib/trpc";
import { PageHeader } from "@/components/ui/PageHeader";
import { StatsCard } from "@/components/ui/StatsCard";
import { DataTable, type Column } from "@/components/ui/DataTable";
import { Plus, FileText, Atom, MessageSquare, Layers } from "lucide-react";
export default function ProjectsPage() {
  const [selectedProject, setSelectedProject] = useState<string | null>(null);
  const { data: projects } = trpc.project.list.useQuery({ workspaceId: "" });
  const overview = trpc.project.assetOverview.useQuery({ id: selectedProject ?? "" }, { enabled: !!selectedProject });
  const columns: Column<any>[] = [
    { key: "name", label: "项目名称", render: (r) => <span className="font-medium">{r.name}</span> },
    { key: "type", label: "类型", render: (r) => <span className="text-xs">{r.type}</span> },
    { key: "owner", label: "负责人", render: (r) => <span className="text-xs">{r.owner?.name}</span> },
    { key: "raws", label: "Raw", render: (r) => <span className="text-xs">{r._count?.raws ?? 0}</span> },
    { key: "atoms", label: "Atoms", render: (r) => <span className="text-xs">{r._count?.atoms ?? 0}</span> },
    { key: "qaPairs", label: "QA", render: (r) => <span className="text-xs">{r._count?.qaPairs ?? 0}</span> },
    { key: "blueprints", label: "蓝图", render: (r) => <span className="text-xs">{r._count?.blueprints ?? 0}</span> },
    { key: "runs", label: "运行", render: (r) => <span className="text-xs">{r._count?.workflowRuns ?? 0}</span> },
  ];
  return (
    <div>
      <PageHeader title="Project 管理" description="项目是资产挂载主线 — 所有资产必须绑定 Project"
        actions={<button className="flex items-center gap-2 rounded-lg bg-admin px-4 py-2 text-sm text-white"><Plus className="h-4 w-4" />新建 Project</button>} />
      <DataTable columns={columns} data={projects ?? []} onRowClick={(r) => setSelectedProject(r.id)} />
      {selectedProject && overview.data && (
        <div className="mt-6">
          <h3 className="text-sm font-semibold mb-3">资产概览</h3>
          <div className="grid grid-cols-4 gap-4">
            <StatsCard title="Raw 素材" value={overview.data.rawCount} icon={<FileText className="h-5 w-5 text-gray-400" />} />
            <StatsCard title="原子块" value={overview.data.atomStats?.reduce((s: number, a: any) => s + a._count, 0) ?? 0} icon={<Atom className="h-5 w-5 text-blue-400" />} color="text-brand" />
            <StatsCard title="QA 对" value={overview.data.qaCount} icon={<MessageSquare className="h-5 w-5 text-purple-400" />} color="text-pf" />
            <StatsCard title="蓝图" value={overview.data.bpStats?.reduce((s: number, a: any) => s + a._count, 0) ?? 0} icon={<Layers className="h-5 w-5 text-oc" />} color="text-oc" />
          </div>
        </div>
      )}
    </div>
  );
}
```

---

## 12. 🏢 Admin-04：用户与角色管理

### src/app/(dashboard)/admin/users/page.tsx

```tsx
"use client";
import { trpc } from "@/lib/trpc";
import { PageHeader } from "@/components/ui/PageHeader";
import { DataTable, type Column } from "@/components/ui/DataTable";
import { Plus } from "lucide-react";
const ROLE_LABELS: Record<string, string> = { SUPER_ADMIN: "超级管理员", TENANT_ADMIN: "租户管理员", KNOWLEDGE_EDITOR: "知识编辑", PROMPT_ENGINEER: "Prompt 工程师", OPERATOR: "运营/投手", REVIEWER: "审核人", READONLY: "只读成员" };
const PERMISSION_MATRIX = [
  { role: "超级管理员", knowledge: "管理", prompts: "管理", orchestration: "管理", governance: "管理", admin: "管理" },
  { role: "租户管理员", knowledge: "管理", prompts: "管理", orchestration: "管理", governance: "管理", admin: "管理" },
  { role: "知识编辑", knowledge: "编辑", prompts: "只读", orchestration: "—", governance: "—", admin: "—" },
  { role: "Prompt 工程师", knowledge: "只读", prompts: "编辑", orchestration: "编辑", governance: "只读", admin: "—" },
  { role: "运营/投手", knowledge: "只读", prompts: "只读", orchestration: "编辑", governance: "只读", admin: "—" },
  { role: "审核人", knowledge: "编辑", prompts: "只读", orchestration: "编辑", governance: "编辑", admin: "—" },
  { role: "只读成员", knowledge: "只读", prompts: "—", orchestration: "只读", governance: "—", admin: "—" },
];
export default function UsersPage() {
  const { data: users, isLoading } = trpc.user.list.useQuery({});
  const updateRole = trpc.user.updateRole.useMutation();
  const userColumns: Column<any>[] = [
    { key: "name", label: "姓名", render: (r) => <span className="font-medium">{r.name}</span> },
    { key: "email", label: "邮箱", render: (r) => <span className="text-xs">{r.email}</span> },
    { key: "role", label: "角色", render: (r) => (
      <select value={r.role} onChange={e => updateRole.mutate({ userId: r.id, role: e.target.value })} className="rounded border px-2 py-1 text-xs">
        {Object.entries(ROLE_LABELS).map(([k, v]) => <option key={k} value={k}>{v}</option>)}
      </select>
    )},
    { key: "tenant", label: "租户", render: (r) => <span className="text-xs">{r.tenant?.name}</span> },
    { key: "status", label: "状态", render: (r) => <span className={`rounded px-2 py-0.5 text-xs ${r.status === "active" ? "bg-green-100 text-green-700" : "bg-gray-100"}`}>{r.status}</span> },
    { key: "lastLoginAt", label: "最后登录", render: (r) => <span className="text-xs text-gray-400">{r.lastLoginAt ? new Date(r.lastLoginAt).toLocaleDateString() : "—"}</span> },
  ];
  return (
    <div>
      <PageHeader title="用户与角色管理" description="RBAC 权限管理与角色矩阵"
        actions={<button className="flex items-center gap-2 rounded-lg bg-admin px-4 py-2 text-sm text-white"><Plus className="h-4 w-4" />添加用户</button>} />
      <DataTable columns={userColumns} data={users ?? []} loading={isLoading} />
      <div className="mt-8">
        <h3 className="text-sm font-semibold mb-3">权限矩阵</h3>
        <div className="overflow-hidden rounded-xl border bg-white">
          <table className="w-full text-xs">
            <thead><tr className="bg-gray-50 border-b"><th className="px-3 py-2 text-left">角色</th><th className="px-3 py-2">知识中心</th><th className="px-3 py-2">提示词工厂</th><th className="px-3 py-2">编排中心</th><th className="px-3 py-2">治理中心</th><th className="px-3 py-2">企业后台</th></tr></thead>
            <tbody>{PERMISSION_MATRIX.map(row => (
              <tr key={row.role} className="border-b">
                <td className="px-3 py-2 font-medium">{row.role}</td>
                {[row.knowledge, row.prompts, row.orchestration, row.governance, row.admin].map((p, i) => (
                  <td key={i} className={`px-3 py-2 text-center ${p === "管理" ? "bg-green-50 text-green-700 font-bold" : p === "编辑" ? "bg-blue-50 text-blue-700" : p === "只读" ? "bg-gray-50 text-gray-500" : "text-gray-300"}`}>{p}</td>
                ))}
              </tr>
            ))}</tbody>
          </table>
        </div>
      </div>
    </div>
  );
}
```

---

## 13. 🏢 Admin-05：资产生命周期管理

### src/app/(dashboard)/admin/lifecycle/page.tsx

```tsx
"use client";
import { PageHeader } from "@/components/ui/PageHeader";
import { StatsCard } from "@/components/ui/StatsCard";
import { PieChart, Pie, Cell, ResponsiveContainer, Tooltip } from "recharts";
import { AlertCircle, Link2Off, ShieldAlert, RefreshCw } from "lucide-react";
const STATUS_PIE = [{ name: "生效中", value: 450, color: "#16A34A" }, { name: "草稿", value: 120, color: "#6B7280" }, { name: "测试中", value: 80, color: "#2563EB" }, { name: "已归档", value: 200, color: "#F59E0B" }, { name: "已过期", value: 30, color: "#DC2626" }];
const TASKS = [
  { type: "有效期到期提醒", count: 8, icon: <AlertCircle className="h-4 w-4 text-orange-500" /> },
  { type: "待核实原子块", count: 15, icon: <ShieldAlert className="h-4 w-4 text-yellow-500" /> },
  { type: "孤儿原子块处理", count: 23, icon: <Link2Off className="h-4 w-4 text-red-500" /> },
  { type: "一致性扫描异常", count: 5, icon: <RefreshCw className="h-4 w-4 text-purple-500" /> },
];
export default function LifecyclePage() {
  return (
    <div>
      <PageHeader title="资产生命周期管理" description="全局视角管理资产健康度、巡检任务与变更联动" />
      <div className="grid grid-cols-4 gap-4 mb-6">
        <StatsCard title="资产总量" value="880" color="text-brand" />
        <StatsCard title="过期率" value="3.4%" subtitle="有效期已过的 Atom/QA" color="text-danger" />
        <StatsCard title="孤儿率" value="5.1%" subtitle="未关联蓝图的 Atom" color="text-orange-600" />
        <StatsCard title="一致性通过率" value="97.2%" subtitle="同源 Atom/QA 无矛盾" color="text-gc" />
      </div>
      <div className="grid grid-cols-2 gap-6 mb-6">
        <div className="rounded-xl border bg-white p-4">
          <h3 className="text-sm font-semibold mb-3">资产状态分布</h3>
          <ResponsiveContainer width="100%" height={250}>
            <PieChart><Pie data={STATUS_PIE} cx="50%" cy="50%" outerRadius={80} dataKey="value" label={({name, value}: any) => `${name}: ${value}`}>
              {STATUS_PIE.map((d, i) => <Cell key={i} fill={d.color} />)}
            </Pie><Tooltip /></PieChart>
          </ResponsiveContainer>
        </div>
        <div className="rounded-xl border bg-white p-4">
          <h3 className="text-sm font-semibold mb-3">巡检任务列表</h3>
          <div className="space-y-3">
            {TASKS.map(t => (
              <div key={t.type} className="flex items-center justify-between rounded-lg bg-gray-50 px-4 py-3">
                <div className="flex items-center gap-3">{t.icon}<span className="text-sm">{t.type}</span></div>
                <div className="flex items-center gap-2">
                  <span className="rounded-full bg-red-100 px-2 py-0.5 text-xs font-bold text-red-600">{t.count}</span>
                  <button className="rounded border px-2 py-1 text-xs hover:bg-white">处理</button>
                </div>
              </div>
            ))}
          </div>
        </div>
      </div>
      <div className="rounded-xl border bg-white p-4">
        <h3 className="text-sm font-semibold mb-3">变更联动日志（最近）</h3>
        <div className="space-y-2">
          {["修改 Raw《产品手册v3》→ 通知 12 个 Atom + 8 个 QA 对", "修改 Atom《竞品分析方法论》→ 通知 3 个 Blueprint 负责人", "归档 QA《旧版FAQ》→ 解除与 5 个 Atom 的关联"].map((log, i) => (
            <div key={i} className="rounded-lg bg-gray-50 px-3 py-2 text-xs text-gray-600">
              <span className="text-gray-400 mr-2">4/{20 - i} 14:{30 + i}</span>{log}
            </div>
          ))}
        </div>
      </div>
    </div>
  );
}
```

---

## 14. 🏢 Admin-06：系统配置

### src/app/(dashboard)/admin/settings/page.tsx

```tsx
"use client";
import { useState } from "react";
import { PageHeader } from "@/components/ui/PageHeader";
import { Save, Tag, Cpu, Wrench, Play, Database, Eye } from "lucide-react";
const SECTIONS = [
  { key: "tags", label: "标签体系配置", icon: <Tag className="h-4 w-4" /> },
  { key: "knowledge", label: "知识加工引擎", icon: <Cpu className="h-4 w-4" /> },
  { key: "assembly", label: "装配引擎配置", icon: <Wrench className="h-4 w-4" /> },
  { key: "runtime", label: "运行时配置", icon: <Play className="h-4 w-4" /> },
  { key: "vector", label: "向量层配置", icon: <Database className="h-4 w-4" /> },
  { key: "observability", label: "可观测性配置", icon: <Eye className="h-4 w-4" /> },
];
export default function SettingsPage() {
  const [active, setActive] = useState("tags");
  return (
    <div>
      <PageHeader title="系统配置" description="标签 / 引擎 / 装配 / 运行时 / 向量 / 可观测性 全配置" />
      <div className="grid grid-cols-5 gap-6">
        <div className="col-span-1 space-y-1">
          {SECTIONS.map(s => (
            <button key={s.key} onClick={() => setActive(s.key)}
              className={`w-full flex items-center gap-2 rounded-lg px-3 py-2.5 text-left text-sm ${active === s.key ? "bg-admin/10 text-admin font-medium" : "hover:bg-gray-50"}`}>
              {s.icon}{s.label}
            </button>
          ))}
        </div>
        <div className="col-span-4 rounded-xl border bg-white p-6">
          {active === "tags" && (
            <div className="space-y-4">
              <h3 className="text-sm font-semibold">标签体系配置</h3>
              <div className="grid grid-cols-2 gap-4">
                <div className="rounded-lg border p-3"><h4 className="text-xs font-semibold mb-2">30 维度枚举值管理</h4><p className="text-xs text-gray-500">编辑维度 1-30 的名称、描述与所属层级映射</p><button className="mt-2 rounded border px-2 py-1 text-xs hover:bg-gray-50">管理维度</button></div>
                <div className="rounded-lg border p-3"><h4 className="text-xs font-semibold mb-2">A/B/C/D 层级定义</h4><p className="text-xs text-gray-500">配置四层级的名称与权限约束</p><button className="mt-2 rounded border px-2 py-1 text-xs hover:bg-gray-50">编辑层级</button></div>
                <div className="rounded-lg border p-3"><h4 className="text-xs font-semibold mb-2">S0-S10 槽位枚举值</h4><p className="text-xs text-gray-500">管理 11 个一级槽位 + 全部 Sx.y 二级槽位</p><button className="mt-2 rounded border px-2 py-1 text-xs hover:bg-gray-50">管理槽位</button></div>
                <div className="rounded-lg border p-3"><h4 className="text-xs font-semibold mb-2">材料类型枚举值</h4><p className="text-xs text-gray-500">管理 12+2 种材料类型定义</p><button className="mt-2 rounded border px-2 py-1 text-xs hover:bg-gray-50">管理类型</button></div>
              </div>
            </div>
          )}
          {active === "knowledge" && (
            <div className="space-y-4">
              <h3 className="text-sm font-semibold">知识加工引擎配置</h3>
              <div className="space-y-3">
                <div><label className="text-xs font-medium">OCR 引擎</label><select className="mt-1 w-full rounded-lg border px-3 py-2 text-sm"><option>Tesseract</option><option>PaddleOCR</option><option>Azure OCR</option></select></div>
                <div><label className="text-xs font-medium">ASR 引擎</label><select className="mt-1 w-full rounded-lg border px-3 py-2 text-sm"><option>Whisper</option><option>阿里达摩院</option></select></div>
                <div className="flex items-center gap-2"><input type="checkbox" defaultChecked /><label className="text-xs">去口语化开关</label></div>
                <div className="grid grid-cols-2 gap-3">
                  <div><label className="text-xs font-medium">切块最小字数</label><input defaultValue="100" className="mt-1 w-full rounded-lg border px-3 py-2 text-sm" /></div>
                  <div><label className="text-xs font-medium">切块最大字数</label><input defaultValue="800" className="mt-1 w-full rounded-lg border px-3 py-2 text-sm" /></div>
                </div>
                <div><label className="text-xs font-medium">自动打标置信度阈值</label><input defaultValue="0.85" className="mt-1 w-full rounded-lg border px-3 py-2 text-sm" /></div>
              </div>
            </div>
          )}
          {active === "assembly" && (
            <div className="space-y-4">
              <h3 className="text-sm font-semibold">装配引擎配置</h3>
              <div><label className="text-xs font-medium">默认冲突优先级</label><input defaultValue="D > C > B > A > 用户偏好" className="mt-1 w-full rounded-lg border px-3 py-2 text-sm" /></div>
              <div><label className="text-xs font-medium">全局 Token 预算上限</label><input defaultValue="8000" className="mt-1 w-full rounded-lg border px-3 py-2 text-sm" /></div>
            </div>
          )}
          {active === "runtime" && (
            <div className="space-y-4">
              <h3 className="text-sm font-semibold">运行时配置</h3>
              <div className="grid grid-cols-2 gap-3">
                <div><label className="text-xs font-medium">3A 质量评分阈值</label><input defaultValue="70" className="mt-1 w-full rounded-lg border px-3 py-2 text-sm" /></div>
                <div><label className="text-xs font-medium">最大重试次数</label><input defaultValue="3" className="mt-1 w-full rounded-lg border px-3 py-2 text-sm" /></div>
              </div>
              <div><label className="text-xs font-medium">语义缓存阈值</label><input defaultValue="0.95" className="mt-1 w-full rounded-lg border px-3 py-2 text-sm" /></div>
            </div>
          )}
          {active === "vector" && (
            <div className="space-y-4">
              <h3 className="text-sm font-semibold">向量层配置</h3>
              <div><label className="text-xs font-medium">嵌入模型</label><select className="mt-1 w-full rounded-lg border px-3 py-2 text-sm"><option>text-embedding-3-large</option><option>BGE-M3</option></select></div>
              <div><label className="text-xs font-medium">向量库</label><select className="mt-1 w-full rounded-lg border px-3 py-2 text-sm"><option>Qdrant</option><option>Milvus</option></select></div>
              <div><label className="text-xs font-medium">重排序模型</label><select className="mt-1 w-full rounded-lg border px-3 py-2 text-sm"><option>BGE-Reranker</option><option>Cohere Rerank</option></select></div>
              <div className="grid grid-cols-2 gap-3">
                <div><label className="text-xs font-medium">Top-K 粗筛</label><input defaultValue="50" className="mt-1 w-full rounded-lg border px-3 py-2 text-sm" /></div>
                <div><label className="text-xs font-medium">Top-N 精排</label><input defaultValue="5" className="mt-1 w-full rounded-lg border px-3 py-2 text-sm" /></div>
              </div>
            </div>
          )}
          {active === "observability" && (
            <div className="space-y-4">
              <h3 className="text-sm font-semibold">可观测性配置</h3>
              <div><label className="text-xs font-medium">Traces 后端</label><select className="mt-1 w-full rounded-lg border px-3 py-2 text-sm"><option>LangSmith</option><option>OpenTelemetry</option></select></div>
              <div><label className="text-xs font-medium">Metrics 后端</label><input defaultValue="http://prometheus:9090" className="mt-1 w-full rounded-lg border px-3 py-2 text-sm" /></div>
              <div><label className="text-xs font-medium">告警 Webhook</label><input defaultValue="" placeholder="企微机器人 Webhook URL" className="mt-1 w-full rounded-lg border px-3 py-2 text-sm" /></div>
              <div className="grid grid-cols-2 gap-3">
                <div><label className="text-xs font-medium">延迟告警阈值 (ms)</label><input defaultValue="5000" className="mt-1 w-full rounded-lg border px-3 py-2 text-sm" /></div>
                <div><label className="text-xs font-medium">红线触发率阈值 (%)</label><input defaultValue="5" className="mt-1 w-full rounded-lg border px-3 py-2 text-sm" /></div>
              </div>
            </div>
          )}
          <div className="mt-6 flex justify-end">
            <button className="flex items-center gap-2 rounded-lg bg-admin px-4 py-2 text-sm text-white hover:bg-admin/90"><Save className="h-4 w-4" />保存配置</button>
          </div>
        </div>
      </div>
    </div>
  );
}
```